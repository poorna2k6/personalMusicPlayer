# Path to your music library folder
MUSIC_LIBRARY_PATH=/path/to/your/music

# Backend server port
PORT=4000

# Database path (default: backend/data/music.db)
# DB_PATH=./backend/data/music.db

# ─── Google OAuth (required for login features) ───────────────────────────────
# Create a project at https://console.developers.google.com
# Enable the "Google Identity" (OAuth 2.0) API, create OAuth credentials
# and add your domain to the allowed origins.
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Secret used to sign JWTs — change this to a strong random string in production
JWT_SECRET=change-me-to-a-long-random-string

# ─── Frontend env vars (create frontend/.env.local) ──────────────────────────
# VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
